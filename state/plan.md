# Plan

## Active objective

Create guardrails for `spring-cleanup` before moving files.

## Next tasks

1. Add seeded Phase 0 files.
2. Update `.gitignore` for secrets, Docker state, Tailscale state.
3. Commit docs-only guardrail patch.
4. Move root scripts into target folders.
5. Deduplicate punctuation normalizer scripts.
6. Add `scripts/link.sh`.
7. Add `scripts/doctor.sh`.

## Blocked

- none
