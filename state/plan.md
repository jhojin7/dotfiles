# Plan

## Active objective

Create guardrails for `spring-cleanup` before moving files.

## Next tasks

1. Update `.gitignore` for secrets, Docker state, Tailscale state.
2. Commit docs-only guardrail patch.
3. Move root scripts into target folders.
4. Deduplicate punctuation normalizer scripts.
5. Add `scripts/link.sh`.
6. Add `scripts/doctor.sh`.

## Blocked

- none
